import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        height: '100%', 
        backgroundColor: '#fff',
        paddingTop: 10
    },
    button: {
        borderRadius: 0,
        borderWidth: 0
    },
    input: {
        backgroundColor: '#fff'
    },
    link: {
        height: 50,
        lineHeight: 40,
        textAlign: 'center'
    }
})

export default styles